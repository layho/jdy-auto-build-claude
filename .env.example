# 简道云登录凭据（仅本地运行使用，严禁提交到 Git）
JDY_USERNAME=your-email@example.com
JDY_PASSWORD=your-password
JDY_API_KEY=your-api-key

# 目标应用（仅允许操作此应用）
JDY_TARGET_APP=爱马仕

# 学习应用（仅允许读取）
JDY_LEARN_APP=进销存

# Browser 配置
BROWSER_HEADLESS=true
BROWSER_TIMEOUT_MS=600000
MAX_RETRY=2
